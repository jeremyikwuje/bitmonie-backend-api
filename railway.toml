[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node_modules/.bin/prisma migrate deploy && echo '>>> migrate done, starting node' && ls -la dist/src/main.js && node dist/src/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
